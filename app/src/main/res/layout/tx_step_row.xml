<?xml version="1.0" encoding="utf-8"?>
<!-- Desktop .tx-step row: 22dp badge (number / spinner / check / cross)
     + label + substatus. Row background + badge tint are swapped in code
     per state (pending/active/ready/confirming/done/failed). -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/linearLayout_tx_step_row"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingTop="10dp"
    android:paddingBottom="10dp"
    android:paddingStart="12dp"
    android:paddingEnd="12dp"
    android:layout_marginBottom="6dp"
    android:background="@drawable/tx_step_row_bg_pending">

    <FrameLayout
        android:layout_width="22dp"
        android:layout_height="22dp">

        <TextView
            android:id="@+id/textView_tx_step_badge"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center"
            android:textSize="12sp"
            android:textStyle="bold"
            android:textColor="@color/colorWhite"
            android:background="@drawable/tx_step_badge_bg" />

        <ProgressBar
            android:id="@+id/progress_tx_step_badge"
            android:layout_width="14dp"
            android:layout_height="14dp"
            android:layout_gravity="center"
            android:indeterminate="true"
            android:indeterminateTint="#C4B5FD"
            android:visibility="gone" />

    </FrameLayout>

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="10dp"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <TextView
            android:id="@+id/textView_tx_step_label"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="14sp"
            android:textColor="@color/colorWhite" />

        <TextView
            android:id="@+id/textView_tx_step_substatus"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="6dp"
            android:textSize="12sp"
            android:textColor="#BFFFFFFF"
            android:visibility="gone" />

    </LinearLayout>

</LinearLayout>
